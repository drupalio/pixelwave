package pixelweave.ui;

import pixelweave.render.command.DisplayList;

public final class RootWidget extends Widget {
    @Override
    protected void render(DisplayList list) {
        // Root acts as container only.
    }
}
